package module10;

public class ETerminalOperations {
    public static void execute() {
        /**
         * Operación terminal:
         * Una vez ejecutada la operación terminal, le indica a nustro stream que tome
         * los items del stream y los pase por los pipes. Y es ahí cuando se van a
         * ejecutar todo.
         * ex. forEahc, count, etc
         * <p>
         * anyMatch() -> Devuelve un Boolean si es que algunos de los items del stream cumple con la condición
         * allMatch() -> Devuelve un Boolean si todos los items del stream cumple con la condición.
         * noneMatch() -> Devuelve un Boolean cuando ningun elemento del stream cumple con la condición.
         * count() -> Te devuleve la cantidad total de items en un stream.
         * findAny() -> Te devuelve el primer elemento que aparezca en el stream. No te garantiza el order. Si no como llega el elemento en el stream.
         * findFrist() -> Te devuelve el primer elemento que puso en el stream.
         * forEach() -> Consumir los elementos de un stream.
         * min() / max() -> Calcular el valor minimo/mayor de un stream por medio de una condición.
         * reduce() -> Función reduce para los streams.
         * toArray() -> Convierte los streams en un array.
         */
    }
}
